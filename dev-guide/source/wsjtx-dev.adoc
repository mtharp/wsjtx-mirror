:prog: The pass:[Developer's] Guide
=== Linux Makefile Example

In order to build _WSJT-X_ form source, install the following packages:

[source,shell]
----------
# DEBIAN, UBUNTU, MINT and FRIENDS
sudo apt-get install gcc g++ gfortran make libfftw3-dev \
libpulse-dev libhamlib-dev qtbase5-dev  pulseaudio subversion \
libgfortran3:i386 libc6-i386 libqt5multimedia5-plugins \
qtmultimedia5-dev
----------

Adjust install method to you distribution package manager.

==== WSJT-X Build and Run Dependencies
[horizontal]
+*gcc*+:: GNU C Compiler
+*g{plus}{plus}*+:: GNU C{plus}{plus} Compiler
+*gfortran*+:: GNU Fortran or F90
+*make*+:: GNU Make
+*libqt5multimedia5-plugins*+:: QT5 Media Plugins
+*libfftw3-dev*+:: Fast Fourier Transforms
+*libpulse-dev*+:: Headers and Files for Pulse AUdio
+*libhamlib-dev*+:: Headers and Files for Hamlib
+*qtbase5-dev*+:: QT5 Base Dev Files
+*qtmultimedia5-dev*+:: Qt5 Multimedia Dev Files
+*pulseaudio*+:: Pulse Audio Sound Driver
+*subversion*+:: Version Control Software
+*libgfortran.so.3*+:: Fortran Library
+*libc.so.6*+:: GCC C Library
+*libm.so.6*+:: GCC C Library
+*libgcc_s.so.1libm.so.6*+:: GCC C Library 
+*libquadmath.so.0*+:: Quad-Precision Math Library

==== Build Methods

IMPORTANT: You can perform the steps manually if desired. Using the script
automates the steps.

* Open a terminal session, +*Ctrl{plus}Alt{plus}T*+
* +*Copy {amp} Paste*+ the following block, then +*Enter*+ to save.

[caption="Create Script: "]
.wsjtx-compile.sh
[source,bash]
----------
include::wsjtx-compile.txt[]
----------

==== If Using The Script, Change Permission and Run
[source,bash]
----------
# CHANGE PERMISSION
chmod +x ./wsjtx-compile.sh

# RUN SCRIPT
./wsjtx-compile.sh
----------

==== Test KVASD

Before you launch _WSJT-X_, ensure _KVASD_ can run.

[source,bash]
----------
# CHANGE DIRECTORY and RUN KVASD
cd wsjtx-$rev_num
./kvasd -v

# SUCESSFUL RETURN
KVASD version 1.11

# VIEW COPYRGHT
./kvasd

----------

==== Run The New WSJT-X Binary

[source,bash]
----------
# When running from a terminal [ ./wsjtx ], the xterm remains open.
# This is good for testing and chasing errors. Run WSJT-X with:

./wsjtx

# You can also use a file manager. Browse to WSJT-X dir, and open
# as you would any other program. Using this method to launch, you
# should not see the terminal window.

----------

=== Windows CMake Example

The critical portion of building _WSJT-X_ on Windows is setting up the
environment and defining paths. Once complete, subsequent builds can be
accomplished in two steps:

* Open +*wsjt-env CMD Terminal*+
* Build _WSJT-X_ with +*wsjtx-build-cmake.bat*+

[source,shell]
---------
wsjtx-build-cmake.bat -r
---------

IMPORTANT: With the exception of {svn}, The entire development environment is
contained within: +*C:\wsjt-env*+. Using the +*wsjt-env CMD Terminal*+ method, any
previous installation of +*QT*+ or +*CMake*+ should not be a problem to either
environment.

==== Directory Setup
* Open Windows CMD Terminal
* Issue the following commands to create the directory tree:
* <<TREE,Figure-1>> is the visual tree.

[[DIRECTORY]]
.C:\wsjt-env\..
[source,shell]
----------
# OPEN WINDOWS CMD TERMINAL
# ONE COMMAND PER LINE - Copy and Paste
c:
mkdir wsjt-env\downloads wsjt-env\fftw3f wsjt-env\hamlib wsjt-env\src
mkdir wsjt-env\wsjtx-build\Debug wsjt-env\wsjtx-build\Release
mkdir wsjt-env\wsjtx-install\Debug wsjt-env\wsjtx-install\Release

# CLOSE CMD WINDOW
----------

* You should end up with the following directory structure:

[[TREE]]
.WSJT Env Directory Tree 
image::images/wsjt-env-tree.png[align="left"]

==== Download Development Packages

* Save the following packages to +*C:\wsjt-env\downloads*+

[horizontal]
+*Qt5 SDK*+:: {qt5_framework} - Qt 5.2.1 Windows-32, (MinGW 4.8, OpenGL)
+*CMake*+:: {cmake} - Win32 Installer
+*Hamlib*+:: {hamlib} - Hamlib 1.2.15.3.zip
+*FFTW3*+:: {fftw3} - 32-bit version: fftw-3.3.3-dll32.zip
+*SVN*+:: {svn} - SilkSVN 1.8.5.x 32-bit
+*MS Tools*+:: {mstools} - Microsoft Tool Kit

.QT5.2.1 Install
* Run the Installer
* Change Install Path to: +*C:\wsjt-env\Qt5*+
* Check: Associate common files types with Qt Creator
* Select Components: Use Default Selections
* License Agreement, read and select
* Start Menu: Use default selections, select next, then install
* Go grab a coffee, this takes about +*5*+ minutes to install
* At Finish, launch Qt Creator to verify it works, then exit.

.CMake Install
* Run the Installer
* Agree to Terms
* Select: Do [red]*#not#* add to system PATH
* Change Install Path to: +*C:\wsjt-env\CMake*+

.SilkSVN Install
* Run the Installer
* Agree to Terms
* Change Install Path to: +*C:\wsjt-env\SlikSvn*+
* Select Typical, then Install
* Note: All the SVN installers tested, added added itself to the system %PATH%

.Extract Hamlib
* Unzip folder hamlib-1.2.15.3.zip
* Rename folder +*hamlib-win32-1.2.15.3 => hamlib*+
* Move folder +*hamlib  =>  C:\wsjt-env\hamlib*+

.Extract FFTW3
* [red]*#Caution#*: FFTW3 does *not* have a top level folder
* Move +*fftw-3.3.3-dll32.zip  =>  C:\wsjt-env\fftw3f*+
* Extract in +*C:\wsjt-env\fftw3f*+

[[MSTOOLS,MS Tools]]
.MS Tools - Win-XP
* Windows Vista and beyond should not need this 
* In a Windows CMD terminal c:\robocopy /?
* If not installed, {mstools} first, then
* Run the installer

==== Setup WSJT-X Build Environment

If you followed this guide, the rest is +*Copy {amp} Paste*+. At the completion of
this section, +*wsjt-env.bat*+ will be your _WSJT-X_ build terminal. For quick
access, create a desktop shortcut.

* Browse to +*C:\wsjt-env*+ and create a file called: +*wsjt-env.bat*+
* Open +*wsjt-env.bat*+ with a text editor, +*Copy {amp} Paste*+ the following
block:
* Then Save and Exit.

[caption="Environment Script: "]
.C:\wsjt-env\wsjtx-env.bat
[source,shell]
----------
include::wsjtx-env.txt[]
----------

* Change directories to: +*C:\wsjt-env*+, Double-Click or Right-Click {amp} Open
+*wsjt-env CMD Terminal*+
* First test that MS RoboCopy is installed: C:\wsjt-env\robocopy /?
* If RoboCopy option are dsplayed, continue, otherwise, install <<MSTOOLS,MS Tools>>
* Check each package version below. Versions may very slightly, but each tool 
should render a version number or the environment is not setup correctly. See
<<TOOLS,Figure-2>> below.

WARNING: You should not proceed until [red]*#all#* of the tools below render
their version from within +*wsjt-env CMD Terminal+* 

[horizontal]
+*cmake*+:: => CMake version 2.8.12
+*qmake*+:: => Qmake version 3.0
+*svn*+:: => SVN version 1.8.5
+*mingw32-make*+:: => Make version 4.8.0
+*gfortran*+:: => GNU Fortran 4.8.0
+*gcc*+:: => gcc 4.8.0
+*g{plus}{plus}*+:: => g++ 4.8.0

[[TOOLS]]
.WSJT-X Build Tools 
image::images/wsjt-env.png[align="left"]

==== Create The CMake Tool-Chain File
* Browse to +*C:\wsjt-env*+, create a file called: +*wsjtx-toolchain.cmake*+
* Open the file with a text editor. +*Copy {amp} Paste*+ the following block:
* Then Save {amp} Exit.

[[TOOLCHAIN]]
[caption="CMake Tool Chain File: "]
.C:\wsjt-env\wsjtx-toolchain.cmake
[source,shell]
----------
include::wsjtx-toolchain.cmake[]
----------

==== Create The CMake Build Script
* Browse to +*C:\wsjt-env*+, create a file called: +*wsjtx-build-cmake.bat*+
* Open the file with a text editor. +*Copy {amp} Paste*+ the following block:
* Then Save {amp} Exit

[[CMAKEBUILD]]
[caption="Build Script: "]
.C:\wsjt-env\wsjtx-build-cmake.bat
[source,shell]
----------
include::wsjtx-build-cmake.txt[]
----------

==== Test Run
* Browse to +*C:\wsjt-env*+, Double-Click or Right-Click {amp} Open
+*wsjt-env.bat*+.
* In the terminal, type

[caption="Build: "]
.Release Version
[source,shell]
---------
wsjtx-build-cmake.bat -r
---------
* New _WSJT-X_ Release Binary Location: +*C:\wsjt-env\wsjtx-install\Release\bin*+

Note the [green]*#Release#\bin* directory

[caption="Build: "]
.Debug Version
[source,shell]
---------
wsjtx-build-cmake.bat -d
---------

* New _WSJT-X_ Debug Binary Location: +*C:\wsjt-env\wsjtx-install\Debug\bin*+

Note the [green]*#Debug#\bin* directory
